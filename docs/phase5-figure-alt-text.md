# Figure alt-text worksheet (Phase 5)

Fill these for accessibility and for embedding in the **portfolio** HTML. Keep each alt-text **self-contained** (a reader who cannot see the figure still gets the main claim).

| File (`figures/`) | Suggested focus for alt-text | Draft alt-text |
|-------------------|------------------------------|----------------|
| `exp_a_perfect_feature.png` | Encoded rate vs $n_c$; Uruguay highlighted; smoothing vs prior strength | |
| `exp_b_smoothing_effect.png` | Test AUC-PR by encoding; train vs test gap | |
| `exp_c_correlation_trap.png` | Scatter TE country vs TE merchant; model bars; mutual information | |
| `exp_d_encoding_comparison.png` | Train vs test AUC-PR: leaky vs proper target encoding | |

**Tips**

- State **axes** or bar categories in words.
- Give **one** numerical takeaway if the figure’s point is quantitative (e.g. “correlation about 0.4 on the training sample”).
- Avoid “figure shows” — start with the **substantive** observation.
