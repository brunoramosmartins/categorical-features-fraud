# Data

This directory contains the synthetic fraud-detection dataset generated by `src/data.py`.

Data files (`.csv`, `.parquet`) are **not tracked by git** — they are generated
deterministically from the parameters in `config.yaml` using a fixed random seed.

To regenerate:

```bash
python scripts/run_all.py
```
