"""Encoding strategies for categorical features.

Implements one-hot, naive target, smoothed target (Bayesian posterior mean),
fold-based target, and deliberately leaky target encoding for comparison.
"""
